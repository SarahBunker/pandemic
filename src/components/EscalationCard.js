import React from "react";

const EscalationCard = () => {
  return (
    <div>
      <p>
        INCREASE THREAT advance the threat level tracker one space. ADD AGENTS
        draw the bottom card from the Threat Deck and add 3 agents to that city.
        INTENSIFY shuffle the cards in the Threat discard pile and put then on
        top of the Threat deck.
      </p>
    </div>
  );
};

export default EscalationCard;
